# s-1 · 2026-07-12

## 做了什么（自动抽取，勿手改）

**改动文件**
- `../../.claude/plans/cryptic-jingling-curry.md`
- `schema/state.schema.json`
- `bin/ratchet-brief`
- `lib/transcript.py`
- `bin/ratchet-context`
- `bin/ratchet-digest`
- `test/run.sh`
- `bin/ratchet-guard`
- `bin/ratchet-state`
- `bin/ratchet-overhead`
- `hooks/hooks.json`
- `.claude-plugin/plugin.json`
- …另有 14 个

**关键命令**

```bash
import json;d=json.load(open('.claude/settings.local.json'));print('keys:',list(d.keys()));print(...
[ -f "$f" ] && printf "%-28s %8s B\n" "$f" "$(wc -c < "$f" | tr -d ' ')"
awk '/^## /{if(h){printf "%-46s %7d B\n", h, c} h=$0; c=0} {c+=length($0)+1} END{if(h)printf "%-4...
jq -r 'keys[]' "$LATEST" 2>/dev/null | sort -u | tr '\n' ' '; echo
jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="tool_use") | .name' "$LA...
[ -z "$HIT" ] && HIT=$(find "$HOME/.npm/_npx" -maxdepth 3 -type d 2>/dev/null | xargs -I{} sh -c ...
codex plugin --help 2>&1 | head -20
codex --help 2>&1 | sed -n '1,40p'
```

**工具**：Bash×89 · Write×26 · Edit×18 · Read×10 · mcp__thinking__sequentialthinking×7 · WebFetch×6 · AskUserQuestion×5 · Glob×3
**轮次**：441
**token**：in 38,359 · out 722,200 · cache 读 134,451,986 / 写 1,348,716 · 合计 136,561,261
**上下文**：46.7% (467,033/1,000,000)

---

## 决策与教训（本节由你手写，≤ 1 KB）

> 只写机器写不出来的东西：为什么这么选、哪里判断错了、下次别再踩什么。
> 不要复述上面已有的机械事实。写不满不用凑。

- **agent 会编造字段名。** 它声称 statusLine 的 stdin 含 `context.percentUsed`，我据此设计了整条链路。实测该字段不存在。子代理/搜索给的字段名、协议细节，写进代码前必须真机验证。已固化为 `.ratchet/rules/`。

- **我判断「Codex 无 hooks」是错的**，实测推翻（有完整 hook + `CLAUDE_PLUGIN_ROOT` 别名，脚本零改复用），`adapters/` 因此砍掉。架构决策若建立在「某平台不支持 X」上，先实测，别信文档摘要。

- **最坏情况测试抓到 schema 上限定错**（初版最坏 3,751 B，超 K1 预算 83%），当场从字节预算反推收紧。没这个测试，简报会在几个月后悄悄膨胀，那时归因极难。

- **GitHub Free 私有仓无 Rulesets** → 服务端不拦任何东西，真门禁只能放本地 pre-push。反而更合 ratchet 的哲学。
